"""SQLAlchemy-style ORM models.

Not used in the MVP (we query SQLite directly via app.database), but kept as
an empty package so future iterations can migrate to SQLAlchemy without
renaming imports.
"""
